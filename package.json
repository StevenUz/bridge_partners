{
  "name": "bridge-partners",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 5000",
    "build": "vite build",
    "preview": "vite preview --host --port 5000"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
