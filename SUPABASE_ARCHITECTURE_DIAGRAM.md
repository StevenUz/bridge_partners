# Server-Authoritative Architecture Diagram

## High-Level Flow

```
┌─────────────────────────────────────────────────────────────┐
│                      CLIENT (Browser)                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   UI Layer   │  │ Event Handler│  │  Game State  │      │
│  │   (React)    │  │  (Realtime)  │  │   (Local)    │      │
│  └──────┬───────┘  └──────▲───────┘  └──────▲───────┘      │
│         │                 │                  │              │
│         │ RPC calls       │ Event stream     │ Snapshot     │
│         └─────────────────┼──────────────────┘              │
└─────────────────────────────│──────────────────────────────┘
                              │
         ═════════════════════╪═════════════════════
                SUPABASE API  │
         ═════════════════════╪═════════════════════
                              │
┌─────────────────────────────▼──────────────────────────────┐
│                    POSTGRES DATABASE                        │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │           RPC FUNCTIONS (SECURITY DEFINER)          │    │
│  │  ┌──────────────┐  ┌──────────────┐               │    │
│  │  │ create_room  │  │  take_seat   │               │    │
│  │  │  join_room   │  │ start_match  │               │    │
│  │  │              │  │ start_board  │               │    │
│  │  └──────┬───────┘  └──────┬───────┘               │    │
│  │         │                 │                        │    │
│  │  ┌──────▼─────────────────▼───────┐               │    │
│  │  │      VALIDATION LOGIC           │               │    │
│  │  │  • Turn order checks            │               │    │
│  │  │  • Follow-suit rules            │               │    │
│  │  │  • Auction ordering             │               │    │
│  │  │  • Card ownership               │               │    │
│  │  └──────┬──────────────────────────┘               │    │
│  └─────────┼──────────────────────────────────────────┘    │
│            │ Validated writes                               │
│  ┌─────────▼────────────────────────────────────────────┐  │
│  │         STATE TABLES (RLS Protected)                  │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐    │  │
│  │  │   rooms    │  │   boards   │  │ game_events│    │  │
│  │  │room_members│  │  auctions  │  │   (log)    │    │  │
│  │  │room_seats  │  │   tricks   │  └────────────┘    │  │
│  │  │            │  │   plays    │                     │  │
│  │  └────────────┘  └────────────┘                     │  │
│  │                                                       │  │
│  │  ┌─────────────────────────────────────────────┐    │  │
│  │  │        HAND PRIVACY (Critical)               │    │  │
│  │  │  ┌────────────────┐  ┌────────────────┐    │    │  │
│  │  │  │ hands_private  │  │  hands_public  │    │    │  │
│  │  │  │                │  │                │    │    │  │
│  │  │  │ RLS: owner_    │  │ RLS: room      │    │    │  │
│  │  │  │ user_id =      │  │ members +      │    │    │  │
│  │  │  │ auth.uid()     │  │ revealed       │    │    │  │
│  │  │  └────────────────┘  └────────────────┘    │    │  │
│  │  └─────────────────────────────────────────────┘    │  │
│  └───────────────────────────────────────────────────────┘│
│                                                             │
│  ┌───────────────────────────────────────────────────────┐│
│  │         SUPABASE REALTIME (Pub/Sub)                   ││
│  │  Listen: INSERT on game_events                        ││
│  │  Broadcast: All clients in room                       ││
│  └───────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

## Data Flow Example: Playing a Card

```
1. CLIENT
   ┌─────────────────────────────────────┐
   │ User clicks card "AS" in UI         │
   └────────────┬────────────────────────┘
                │
                ▼
   ┌─────────────────────────────────────┐
   │ await supabase.rpc('play_card', {   │
   │   p_board_id: uuid,                 │
   │   p_card: 'AS'                      │
   │ })                                  │
   └────────────┬────────────────────────┘
                │
   ═════════════╪═════════════════════════
                │ HTTPS Request
   ═════════════╪═════════════════════════
                │
2. POSTGRES RPC (SECURITY DEFINER)
                ▼
   ┌─────────────────────────────────────┐
   │ function play_card()                │
   │                                     │
   │ 1. Lock board (FOR UPDATE)          │
   │ 2. Get user_seat from room_seats    │
   │ 3. Validate turn (current_turn ==   │
   │    user_seat) ✓                     │
   │ 4. Get user's hand from             │
   │    hands_private                    │
   │ 5. Validate card in hand ✓          │
   │ 6. Get lead suit from trick         │
   │ 7. Validate follow-suit ✓           │
   │ 8. Remove card from hands_private   │
   │ 9. INSERT INTO plays                │
   │ 10. Update current_turn_seat        │
   │ 11. INSERT INTO game_events         │
   │ 12. COMMIT transaction              │
   └────────────┬────────────────────────┘
                │
                ▼
   ┌─────────────────────────────────────┐
   │ TRIGGER: game_events INSERT         │
   │   ↓                                 │
   │ Supabase Realtime broadcasts        │
   └────────────┬────────────────────────┘
                │
   ═════════════╪═════════════════════════
                │ WebSocket (all clients)
   ═════════════╪═════════════════════════
                │
3. ALL CLIENTS (in room)
                ▼
   ┌─────────────────────────────────────┐
   │ Event received:                     │
   │ {                                   │
   │   event_type: 'card_played',        │
   │   event_data: {                     │
   │     seat: 0,                        │
   │     card: 'AS',                     │
   │     trick: 3                        │
   │   }                                 │
   │ }                                   │
   └────────────┬────────────────────────┘
                │
                ▼
   ┌─────────────────────────────────────┐
   │ handleEvent() triggers              │
   │   ↓                                 │
   │ Refresh snapshot:                   │
   │ await supabase.rpc('room_snapshot') │
   │   ↓                                 │
   │ Update UI with new state            │
   └─────────────────────────────────────┘
```

## Hand Privacy Flow

```
┌──────────────────────────────────────────────────────────────┐
│                         4 PLAYERS                             │
│  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐            │
│  │ North  │  │  East  │  │ South  │  │  West  │            │
│  │(User A)│  │(User B)│  │(User C)│  │(User D)│            │
│  └───┬────┘  └───┬────┘  └───┬────┘  └───┬────┘            │
│      │           │           │           │                   │
│      │ SELECT * FROM hands_private      │                   │
│      └───────────┴───────────┴───────────┘                   │
│                  │                                            │
└──────────────────┼────────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│              POSTGRES RLS POLICY                              │
│  WHERE owner_user_id = auth.uid()                            │
│                                                               │
│  User A sees: [AH, KH, QH, ...]  ✓                          │
│  User B sees: [2S, 3S, 4S, ...]  ✓                          │
│  User C sees: [10D, JD, QD, ...] ✓                          │
│  User D sees: [5C, 6C, 7C, ...]  ✓                          │
│                                                               │
│  User A CANNOT see B/C/D hands   ❌                          │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│              DUMMY REVEAL (After Opening Lead)                │
│                                                               │
│  play_card() RPC function:                                   │
│    IF first_card AND trick_number = 1:                       │
│      INSERT INTO hands_public                                │
│        SELECT * FROM hands_private                           │
│        WHERE seat = dummy_seat                               │
│                                                               │
│      UPDATE boards SET dummy_revealed = true                 │
│                                                               │
│  Now all players can SELECT from hands_public                │
│  (RLS allows room members)                                   │
└──────────────────────────────────────────────────────────────┘
```

## Turn Order Validation

```
┌──────────────────────────────────────────────────────────────┐
│                    AUCTION PHASE                              │
│                                                               │
│  Dealer starts (seat = dealer_seat)                          │
│  Rotation: dealer → +1 → +2 → +3 → dealer → ...             │
│                                                               │
│  boards.current_turn_seat tracks whose turn it is            │
│                                                               │
│  submit_call() validates:                                    │
│    IF current_turn_seat != user_seat:                        │
│      RAISE EXCEPTION 'Not your turn'                         │
│                                                               │
│  After each call:                                            │
│    current_turn_seat = (current_turn_seat + 1) % 4           │
│                                                               │
│  Auction ends when 3 passes after ≥1 bid                     │
│  Sets opening_leader, transitions to play                    │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                     PLAY PHASE                                │
│                                                               │
│  Opening leader starts trick 1                               │
│  Then: leader → +1 → +2 → +3 (4 cards)                      │
│                                                               │
│  play_card() validates:                                      │
│    IF current_turn_seat != user_seat:                        │
│      RAISE EXCEPTION 'Not your turn'                         │
│                                                               │
│  After each card:                                            │
│    current_turn_seat = (current_turn_seat + 1) % 4           │
│                                                               │
│  After 4th card:                                             │
│    winner = determine_trick_winner()                         │
│    current_turn_seat = winner (for next trick)               │
└──────────────────────────────────────────────────────────────┘
```

## Follow-Suit Validation

```
┌──────────────────────────────────────────────────────────────┐
│         play_card() Follow-Suit Logic                        │
│                                                               │
│  1. Get lead suit (first card of trick)                     │
│     lead_suit = plays[sequence=1].card_suit                  │
│                                                               │
│  2. Get player's hand                                        │
│     hand = hands_private WHERE seat = user_seat              │
│                                                               │
│  3. Check if player has lead suit                           │
│     has_lead = EXISTS (card IN hand WHERE suit = lead)       │
│                                                               │
│  4. If has lead suit, must play it                          │
│     IF has_lead AND played_card.suit != lead:                │
│       RAISE EXCEPTION 'Must follow suit'                     │
│                                                               │
│  5. If no lead suit, can play any card                      │
│     (discard or trump)                                       │
└──────────────────────────────────────────────────────────────┘
```

## Trick Winner Determination

```
┌──────────────────────────────────────────────────────────────┐
│      determine_trick_winner(trick_id, board_id)              │
│                                                               │
│  1. Get trump suit from final_contract_strain                │
│     (clubs/diamonds/hearts/spades/notrump)                   │
│                                                               │
│  2. Get lead suit (first card played)                       │
│                                                               │
│  3. Iterate through 4 plays:                                │
│     FOR each card:                                           │
│       IF trump suit AND card is trump:                       │
│         Winner = highest trump                               │
│       ELSE:                                                  │
│         Winner = highest card in lead suit                   │
│                                                               │
│  4. Rank order: 2 < 3 < ... < 10 < J < Q < K < A            │
│                                                               │
│  5. Return winner_seat (0-3)                                │
└──────────────────────────────────────────────────────────────┘
```

## Security Layers

```
┌──────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                            │
│                                                               │
│  Layer 1: Authentication (Supabase Auth)                     │
│    ↓                                                          │
│    User must be signed in to call RPCs                       │
│    auth.uid() identifies user                                │
│                                                               │
│  Layer 2: Row Level Security (Postgres RLS)                 │
│    ↓                                                          │
│    hands_private: WHERE owner_user_id = auth.uid()           │
│    hands_public: WHERE is_room_member()                      │
│    All tables: SELECT only (no INSERT/UPDATE/DELETE)         │
│                                                               │
│  Layer 3: RPC Validation (SECURITY DEFINER)                 │
│    ↓                                                          │
│    Bypasses RLS to write                                     │
│    Validates all business rules:                             │
│      • Turn order                                            │
│      • Follow suit                                           │
│      • Card ownership                                        │
│      • Auction ordering                                      │
│      • Seat availability                                     │
│                                                               │
│  Layer 4: Transaction Locking (FOR UPDATE)                  │
│    ↓                                                          │
│    Prevents concurrent modifications                         │
│    Ensures atomic state updates                              │
│    Serializes conflicting operations                         │
│                                                               │
│  Result: CHEAT-PROOF ✅                                       │
└──────────────────────────────────────────────────────────────┘
```

## Event-Driven Sync

```
┌──────────────────────────────────────────────────────────────┐
│              Event-Driven Architecture                        │
│                                                               │
│  Every RPC function:                                         │
│    1. Validates action                                       │
│    2. Updates state tables                                   │
│    3. Inserts into game_events                               │
│    4. Commits transaction                                    │
│       ↓                                                       │
│    Supabase Realtime triggers                                │
│       ↓                                                       │
│    All subscribed clients receive event                      │
│       ↓                                                       │
│    Clients call room_snapshot()                              │
│       ↓                                                       │
│    UI re-renders with new state                              │
│                                                               │
│  Benefits:                                                   │
│    • Single source of truth (database)                       │
│    • Automatic sync (no polling)                             │
│    • Reconnection support (fetch missed events)              │
│    • Audit trail (full game history)                         │
│    • Replay capability (reconstruct any state)               │
└──────────────────────────────────────────────────────────────┘
```

## Comparison: Traditional vs Server-Authoritative

```
┌─────────────────────────────────────────────────────────────┐
│           TRADITIONAL ARCHITECTURE (Insecure)                │
│                                                              │
│  Client A                    Client B                       │
│     ↓                           ↓                           │
│  Frontend validates          Frontend validates            │
│     ↓                           ↓                           │
│  Direct DB write            Direct DB write                │
│     ↓                           ↓                           │
│  ❌ Can write any data       ❌ Can write any data          │
│  ❌ Can see all hands        ❌ Can modify turn order       │
│  ❌ Can skip validation      ❌ Can play any card           │
│                                                              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│         SERVER-AUTHORITATIVE ARCHITECTURE (Secure)           │
│                                                              │
│  Client A                    Client B                       │
│     ↓                           ↓                           │
│  Calls RPC                   Calls RPC                      │
│     ↓                           ↓                           │
│     └───────────┬───────────────┘                           │
│                 ▼                                            │
│           Postgres RPC                                      │
│         (Single validator)                                  │
│                 ↓                                            │
│         ✅ Validates ALL rules                               │
│         ✅ Enforces hand privacy                             │
│         ✅ Prevents cheating                                 │
│         ✅ Atomic transactions                               │
│                 ↓                                            │
│           Updates state                                     │
│                 ↓                                            │
│         Broadcasts to all                                   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

This architecture ensures that **no client can cheat**, all game rules are **consistently enforced**, and **hand privacy is guaranteed** at the database level. 🎯
