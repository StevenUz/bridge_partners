{
  "name": "bridge-partners",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 5000",
    "build": "vite build",
    "preview": "vite preview --host --port 5000",
    "supabase:pull": "powershell -ExecutionPolicy Bypass -File ./scripts/pull-migrations.ps1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.94.1",
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
